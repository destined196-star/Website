{
  "name": "devimurlika-site",
  "version": "1.0.0",
  "description": "Devi Murlika Gaur — devotional site with admin panel",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "lint": "node --check server.js && node --check db.js && echo '✓ Syntax OK'",
    "test": "node --check server.js && node --check db.js && node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "13.0.1",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "express-session": "^1.18.1",
    "helmet": "^8.2.0",
    "multer": "^2.1.1",
    "nodemailer": "^9.0.1",
    "otplib": "^13.4.1",
    "qrcode": "^1.5.4"
  }
}
